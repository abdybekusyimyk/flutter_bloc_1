import 'package:flutter/material.dart';

class Sabak extends StatelessWidget {
  const Sabak({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
